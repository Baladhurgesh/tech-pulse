# OpenAI API Key for article summarization
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Supabase Configuration (Required)
# Get these from: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# Use either the new publishable key format or the legacy anon key
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_xxx
# Or legacy format: NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxx
# Optional: Service role key for admin operations (if not set, uses publishable key)
# SUPABASE_SERVICE_ROLE_KEY=eyJxxx

# Cron Secret (for scheduled ingestion)
# Generate with: openssl rand -hex 32
CRON_SECRET=your-cron-secret-here

# GitHub Repository URL (shown in header)
NEXT_PUBLIC_GITHUB_URL=https://github.com/your-username/news-summarizer

# Optional: Cache duration in minutes (default: 10)
CACHE_TTL_MINUTES=10

# Optional: Number of top stories to fetch (default: 30)
TOP_STORIES_COUNT=30

# Copy this file to .env.local and fill in your values:
# cp env.example .env.local
